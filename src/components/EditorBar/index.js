import React from "react";
import "./index.css"

function EditorBar(props) {
    const view = (
        <div className={props.className + ' wrapper-editor-bar'}>
            TODO:// toolbar
        </div>
    );
    return view;
}

export default EditorBar;
